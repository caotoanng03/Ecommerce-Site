mixin search(keyword)
    form(id="form-search")
        .input-group
            input(
                type="text"
                placeholder="Enter keyword..."
                name="keyword"
                class="form-control"
                value=keyword
                autocomplete
            )
            .input-group-append
                button(
                class="btn btn-success"
                type="submit"
            ) Search